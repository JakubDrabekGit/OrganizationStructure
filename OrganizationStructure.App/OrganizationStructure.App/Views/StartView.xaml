<UserControl x:Class="OrganizationStructure.App.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrganizationStructure.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <!--<RowDefinition />-->
        </Grid.RowDefinitions>


        <TextBlock Grid.Row="0" Text="Organization Structure" Style="{StaticResource HeaderText}" />
        <!--<Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"  Content="CREATE" Command="{Binding CreateCommand}" Width="200" Height="100" />
            <Button Grid.Column="1" Content="LOAD" Command="{Binding LoadCommand}" Width="200" Height="100" />
        </Grid>-->
    </Grid>
</UserControl>
